name: Discord Commit Notifications

on:
  push:
    branches:
      - "**"

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Send message to Discord
        uses: Ilshidur/action-discord@<FULL_40_CHAR_SHA>
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
